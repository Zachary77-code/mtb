# 核心框架（仅 LangGraph 1.0.6，完全不使用 LangChain）
langgraph==1.0.6

# 数据模型
pydantic==2.9.2
typing-extensions==4.12.2

# OpenAI API
openai>=1.55.0
python-dotenv==1.0.1

# HTTP 客户端（确保与 OpenAI SDK 兼容）
httpx>=0.27.0,<0.28.0

# HTML 生成
jinja2==3.1.4
markdown==3.7
PyYAML>=6.0

# HTTP 请求（工具占位符使用）
requests==2.32.3

# 日志
loguru==0.7.2

# RAG 依赖
chromadb>=0.4.0
sentence-transformers>=2.2.0
PyMuPDF>=1.23.0

# XML 解析 (PubMed/ClinVar)
lxml>=4.9.0

# 测试（可选）
pytest==8.3.3
